apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app_name }}
  namespace: demo
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.app_name }}
  ports:
    - name: http
      port: 80
      targetPort: {{ .Values.container_port }}
      protocol: TCP
